---
phase: 06-business-first-class
plan: 03
type: execute
wave: 3
depends_on: ["06-02"]
files_modified:
  - alert/templates.py
  - .github/workflows/premium_cabin_monitor.yml
autonomous: true

must_haves:
  truths:
    - "Premium cabin alert emails clearly show cabin class (Business/First/Premium Economy) in subject and body"
    - "GitHub Actions workflow runs premium cabin monitor every 5 hours on separate schedule from economy"
    - "Workflow passes all required secrets and env vars to premium_cabin_monitor.py"
    - "premium_budget.json committed to repo for persistence across workflow runs"
  artifacts:
    - path: "alert/templates.py"
      provides: "format_premium_cabin_subject() and format_premium_cabin_card_html() functions"
      contains: "premium_cabin"
    - path: ".github/workflows/premium_cabin_monitor.yml"
      provides: "GitHub Actions workflow running every 5 hours"
      contains: "premium_cabin_monitor"
  key_links:
    - from: ".github/workflows/premium_cabin_monitor.yml"
      to: "premium_cabin_monitor.py"
      via: "python premium_cabin_monitor.py entrypoint"
      pattern: "python premium_cabin_monitor.py"
    - from: "alert/templates.py"
      to: "subscriber/router.py"
      via: "AlertRouter._send_instant_email() uses templates"
      pattern: "format_alert_subject"
---

<objective>
Add premium cabin email templates and create the GitHub Actions workflow to run premium cabin monitoring every 5 hours.

Purpose: This plan makes premium cabin monitoring operational. Without the workflow, monitoring doesn't run. Without the templates, alerts don't distinguish cabin class from economy.

Output: Extended alert/templates.py with premium cabin templates, new .github/workflows/premium_cabin_monitor.yml.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-business-first-class/06-CONTEXT.md
@.planning/phases/06-business-first-class/06-RESEARCH.md
@.planning/phases/06-business-first-class/06-01-SUMMARY.md
@.planning/phases/06-business-first-class/06-02-SUMMARY.md

Key files to read before implementing:
@alert/templates.py (extend with premium cabin templates)
@.github/workflows/priority_monitor.yml (pattern reference for workflow structure)
@premium_cabin_monitor.py (Plan 02 output — the entry point the workflow calls)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Premium cabin email templates</name>
  <files>alert/templates.py</files>
  <action>
  Add premium cabin alert template functions to alert/templates.py:

  1. Add cabin class display constants after the existing TIER_EMOJIS:

  ```python
  CABIN_CLASS_DISPLAY = {
      "BUSINESS": {"label": "Business Class", "badge_bg": "#1E40AF", "badge_text": "#FFF", "emoji": "BIZ"},
      "FIRST": {"label": "First Class", "badge_bg": "#7C2D12", "badge_text": "#FFF", "emoji": "1ST"},
      "PREMIUM_ECONOMY": {"label": "Premium Economy", "badge_bg": "#065F46", "badge_text": "#FFF", "emoji": "PE"},
  }
  ```

  2. Add `format_premium_cabin_subject()`:

  ```python
  def format_premium_cabin_subject(
      dest_name: str,
      price_cents: int,
      cabin_class: str,
      normal_price_cents: Optional[int] = None,
  ) -> str:
  ```

  Subject formats:
  - Normal: `"[BIZ] Business Class Deal: Lagos from $2,400"`
  - With savings: `"[BIZ] Business Class Deal: Lagos $2,400 (40% off)"`
  - First class: `"[1ST] First Class Deal: Lagos from $4,000"`
  - Premium economy: `"[PE] Premium Economy Deal: Accra from $960"`

  Include savings percentage if `normal_price_cents` is provided and > 0.

  3. Add `format_premium_cabin_card_html()`:

  ```python
  def format_premium_cabin_card_html(deal: dict) -> str:
  ```

  Build an HTML deal card following the existing Detty design system (same border-radius, padding, font-family as `_build_digest_deal_card()`) but with:
  - Cabin class badge at top (colored pill: blue for Business, brown for First, green for Premium Economy)
  - Price prominently displayed with normal price strikethrough
  - Savings percentage
  - Route (origin -> dest_name)
  - Departure date
  - Book Now button (red CTA, same as existing)
  - Urgency messaging: "Premium cabin deals are rare. This price may not last."

  4. Add `build_premium_cabin_alert_html()`:

  ```python
  def build_premium_cabin_alert_html(
      subscriber_name: str,
      deal: dict,
  ) -> str:
  ```

  Build a complete standalone email for a single premium cabin deal. Structure:
  - Detty header (same gradient branding)
  - "Premium Cabin Alert" subheader
  - Greeting
  - Single deal card (from format_premium_cabin_card_html)
  - Footer with "You're a Premium member" messaging

  5. Add `build_premium_cabin_alert_plain()`:

  ```python
  def build_premium_cabin_alert_plain(
      subscriber_name: str,
      deal: dict,
  ) -> str:
  ```

  Plain text version with the same content.

  IMPORTANT: These templates are used by AlertRouter._send_instant_email(). The router checks `deal.get("cabin_class")` to determine which template path to use. Update `_send_instant_email()` in subscriber/router.py to check for cabin_class and use the premium cabin templates when present. Wait — subscriber/router.py is NOT in this plan's files_modified. Instead, premium_cabin_monitor.py from Plan 02 should handle its own email sending for premium cabin deals, bypassing the generic AlertRouter path if needed. Actually, the cleanest approach: have premium_cabin_monitor.py call AlertRouter.route_deal() with the deal dict including `cabin_class`, and have AlertRouter detect `cabin_class` to use the right template. But that modifies router.py which is Plan 05's file.

  REVISED APPROACH: Add a helper function `send_premium_cabin_alert(subscriber_email, subscriber_name, deal)` to alert/templates.py that handles the full send. The premium_cabin_monitor.py will call this directly instead of going through AlertRouter for email formatting. AlertRouter is still used for subscriber filtering and SMS, but the email body comes from this helper. This avoids modifying router.py.

  Actually, the SIMPLEST approach: In premium_cabin_monitor.py (Plan 02), when a deal is detected, build the deal dict with the right fields and call `AlertRouter.route_deal()`. The AlertRouter calls `_send_instant_email()` which calls `build_email_content()`. The deal dict will have `cabin_class` set. We can add a check in deal_finder's `build_email_content()` or in `format_destination_card_html()` for cabin_class. BUT those are also not in this plan's files_modified.

  FINAL APPROACH: Keep it simple. Add the template functions to alert/templates.py. Then in premium_cabin_monitor.py, after AlertRouter identifies which subscribers to send to, the monitor sends emails directly using these templates (not through AlertRouter._send_instant_email). This means premium_cabin_monitor.py handles its own email delivery for premium cabin deals. This is the cleanest separation. Plan 02 already has premium_cabin_monitor.py in its files_modified, but Plan 02 may not have included direct email sending. The premium_cabin_monitor.py from Plan 02 can call `self._router.route_deal(deal)` which handles subscriber filtering, and then we need the email to look right.

  OK, simplest path that works: Just add the template functions. The premium_cabin_monitor.py will import and use `format_premium_cabin_subject()` to override the subject, and `format_premium_cabin_card_html()` for the body. It will use `mvp0_sender.send_to_subscriber()` directly (same as AlertRouter does). The routing logic (who to send to) comes from AlertRouter's subscriber filtering. We just need the email content to be cabin-class-aware.

  Add one more function: `build_premium_cabin_email(deal: dict) -> tuple[str, str, str]` that returns (subject, plain_body, html_body) ready for sending. This is the convenience function that premium_cabin_monitor.py calls.
  </action>
  <verify>
  Run: `python -c "
from alert.templates import (
    CABIN_CLASS_DISPLAY,
    format_premium_cabin_subject,
    format_premium_cabin_card_html,
    build_premium_cabin_alert_html,
    build_premium_cabin_alert_plain,
    build_premium_cabin_email,
)
print('All premium cabin template functions imported')

subj = format_premium_cabin_subject('Lagos', 240000, 'BUSINESS', normal_price_cents=400000)
print('Subject:', subj)

deal = {
    'origin': 'JFK', 'dest': 'LOS', 'dest_name': 'Lagos',
    'price': 2400, 'cabin_class': 'BUSINESS',
    'normal_price': 4000, 'departure_date': '2026-05-15',
    'url': 'https://google.com/flights'
}
card = format_premium_cabin_card_html(deal)
print('Card HTML length:', len(card))
assert 'Business Class' in card, 'Missing cabin class label'

subject, plain, html = build_premium_cabin_email(deal)
print('Email subject:', subject)
print('Plain text length:', len(plain))
print('HTML length:', len(html))
assert 'Business Class' in html
assert 'Business Class' in plain
print('All assertions passed')
"` — All functions work, cabin class shown in output.
  </verify>
  <done>
  - CABIN_CLASS_DISPLAY dict with styling for BUSINESS, FIRST, PREMIUM_ECONOMY
  - format_premium_cabin_subject() produces cabin-class-aware subject lines
  - format_premium_cabin_card_html() produces styled deal cards with cabin class badge
  - build_premium_cabin_alert_html/plain() produce complete email documents
  - build_premium_cabin_email() convenience function returns (subject, plain, html) tuple
  </done>
</task>

<task type="auto">
  <name>Task 2: GitHub Actions workflow for premium cabin monitoring</name>
  <files>.github/workflows/premium_cabin_monitor.yml</files>
  <action>
  Create `.github/workflows/premium_cabin_monitor.yml` following the pattern of `priority_monitor.yml` but with these differences:

  1. Schedule: every 5 hours at :45 past the hour (offset from economy monitor's :15)
     ```yaml
     cron: '45 */5 * * *'  # Every 5 hours at :45 past (offset from economy :15)
     ```
     5 hours gives ~4.8 runs/day * 30 days = ~144 runs/month. At 216 calls/run (6 routes * 3 cabins * 12 dates) = ~31,000 calls/month. That exceeds the 5,000 budget. But the PremiumBudget tracker will enforce the cap — runs will be skipped once budget is exhausted. The workflow runs but exits early. This is fine — the budget tracker handles it.

     Wait: 6 routes * 3 cabins * 12 dates = 216 calls/run. Budget = 5,000 calls/month. That's only ~23 full runs/month, or about 1 run every 31 hours. That's too infrequent.

     REVISED: Reduce sample dates from 12 to 6 for premium cabins (every 4 weeks instead of every 2 weeks). 6 routes * 3 cabins * 6 dates = 108 calls/run. 5,000/108 = ~46 runs/month. That's about 1 run every 15.6 hours. Still not enough for 5-hour cadence.

     REVISED AGAIN: The budget tracker will handle this. Set the workflow to run every 5 hours. Most runs will complete (budget permitting). When budget runs out mid-month, runs exit immediately. This is the design from CONTEXT.md: "stop checks when budget hit, resume next month." The 5-hour schedule is the MAXIMUM frequency; actual frequency is budget-limited.

     To make more runs fit: use 6 sample dates (not 12) for premium cabins. Update Plan 02's premium_cabin_monitor.py to use `generate_sample_dates(num_dates=6)`. That's 6 * 3 * 6 = 108 calls/run. At 5000/108 ≈ 46 runs = about one every 15.5 hours on average. Better but still limited. Alternatively, we can skip FIRST class for routes where it's consistently empty after the first few runs, bringing it down to 6 * 2 * 6 = 72 calls/run = 69 runs/month = one every 10 hours.

     Bottom line: Keep the schedule at every 5 hours. Budget tracker will short-circuit. As baselines build and FIRST class is confirmed absent on certain routes, the system naturally becomes more efficient.

  2. Workflow name: "Premium Cabin Monitor"

  3. Same concurrency group as economy: `detty-state-commit` (they share state files)

  4. Env vars:
     - All the same secrets as priority_monitor.yml (Amadeus, SMTP, Turso, Google Sheets)
     - `PREMIUM_CABIN_MONITORING_ENABLED: 'true'` (feature flag)
     - `AMADEUS_HOSTNAME: test` (switch to production when ready)

  5. Steps:
     - Checkout
     - Setup Python 3.11
     - Install dependencies
     - Check Turso connection (same pattern, continue-on-error)
     - Run premium cabin monitor: `python premium_cabin_monitor.py`
     - Commit state files: `premium_budget.json price_cache.json alert_cooldown.json price_history.jsonl`

  6. Add workflow_dispatch for manual testing.

  7. Same permissions: `contents: write` for committing state files.
  </action>
  <verify>
  Run: `python -c "
import yaml
with open('.github/workflows/premium_cabin_monitor.yml') as f:
    wf = yaml.safe_load(f)
print('Workflow name:', wf.get('name'))
print('Schedule:', wf.get('on', {}).get('schedule'))
print('Concurrency group:', wf.get('concurrency', {}).get('group'))
steps = wf.get('jobs', {}).get('monitor', {}).get('steps', [])
step_names = [s.get('name', '') for s in steps]
print('Steps:', step_names)
assert 'Run premium cabin monitor' in step_names, 'Missing monitor step'
assert 'Commit state files' in step_names, 'Missing commit step'
print('Workflow structure valid')
"` — Verify workflow YAML parses correctly and has expected structure.

  If yaml not installed, verify manually: `head -30 .github/workflows/premium_cabin_monitor.yml` should show cron schedule, concurrency group, and job structure.
  </verify>
  <done>
  - premium_cabin_monitor.yml workflow created with 5-hour schedule
  - All required secrets passed as env vars
  - PREMIUM_CABIN_MONITORING_ENABLED feature flag set
  - Concurrency group shared with economy monitor (state file safety)
  - premium_budget.json committed for persistence across runs
  - workflow_dispatch enabled for manual testing
  </done>
</task>

</tasks>

<verification>
1. Premium cabin email templates produce cabin-class-aware subjects and HTML bodies
2. Workflow YAML is valid and references correct entry point (premium_cabin_monitor.py)
3. Workflow runs on 5-hour schedule, offset from economy monitor
4. Workflow commits premium_budget.json for state persistence
5. Feature flag (PREMIUM_CABIN_MONITORING_ENABLED) is passed to the monitor
</verification>

<success_criteria>
- Premium cabin alerts clearly distinguish Business/First/Premium Economy class in email subject and body
- GitHub Actions workflow runs premium_cabin_monitor.py on 5-hour cadence
- Workflow shares concurrency group with economy monitor for safe state file commits
- Premium cabin monitoring is fully operational end-to-end when all three plans are complete
</success_criteria>

<output>
After completion, create `.planning/phases/06-business-first-class/06-03-SUMMARY.md`
</output>
