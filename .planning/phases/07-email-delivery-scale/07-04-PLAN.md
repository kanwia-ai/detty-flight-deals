---
phase: 07-email-delivery-scale
plan: 04
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - subscriber/router.py
  - subscriber/digest.py
  - .github/workflows/send_welcome_email.yml
  - .github/workflows/find_deals.yml
  - .github/workflows/priority_monitor.yml
  - .github/workflows/premium_cabin_monitor.yml
  - .github/workflows/weekly_digest.yml
  - .github/workflows/mistake_fares.yml
autonomous: true

must_haves:
  truths:
    - "Gmail 90/day safety limit removed or raised for Resend"
    - "Welcome email workflow uses mvp0_sender module instead of inline SMTP"
    - "All 6 email-sending workflows include RESEND_API_KEY secret"
    - "All 6 email-sending workflows include UNSUBSCRIBE_SECRET secret"
    - "All 6 email-sending workflows include SEND_DOMAIN env var"
  artifacts:
    - path: "subscriber/router.py"
      provides: "Updated email send limit for Resend"
      contains: "RESEND"
    - path: "subscriber/digest.py"
      provides: "Updated daily limit constant for Resend"
      contains: "RESEND"
    - path: ".github/workflows/send_welcome_email.yml"
      provides: "Welcome email via mvp0_sender module (not inline SMTP)"
      contains: "mvp0_sender"
  key_links:
    - from: ".github/workflows/*.yml"
      to: "email_client.py"
      via: "RESEND_API_KEY environment variable"
      pattern: "RESEND_API_KEY"
    - from: ".github/workflows/send_welcome_email.yml"
      to: "mvp0_sender.py:send_welcome_email()"
      via: "python module import"
      pattern: "mvp0_sender"
---

<objective>
Remove Gmail daily limits, refactor the welcome email workflow to use the shared module, and add Resend secrets to all GitHub Actions workflows.

Purpose: The Gmail 90/day safety limit in router.py and digest.py is no longer needed with Resend (Pro tier has no daily limit). The welcome email workflow has inline SMTP code that must be refactored to use mvp0_sender.py (which now delegates to email_client.py). All 6 workflows need RESEND_API_KEY, UNSUBSCRIBE_SECRET, and SEND_DOMAIN environment variables.

Output: Updated router.py, digest.py, welcome email workflow, and all 6 email-sending workflows with Resend configuration.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-email-delivery-scale/07-RESEARCH.md
@.planning/phases/07-email-delivery-scale/07-01-SUMMARY.md
@subscriber/router.py
@subscriber/digest.py
@.github/workflows/send_welcome_email.yml
@.github/workflows/find_deals.yml
@.github/workflows/weekly_digest.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove Gmail daily limits and update to Resend-aware limits</name>
  <files>subscriber/router.py, subscriber/digest.py</files>
  <action>
1. **`subscriber/router.py`** (line ~128):
   - Current: `if self._email_send_count >= 90:` with comment about Gmail limit
   - Change to: `DAILY_EMAIL_LIMIT = int(os.environ.get("DAILY_EMAIL_LIMIT", "500"))`
   - Update the condition: `if self._email_send_count >= DAILY_EMAIL_LIMIT:`
   - Update the log message to reference the configurable limit (not "Gmail")
   - Add `import os` if not already imported
   - Comment: `# Resend Pro: no daily limit. Free tier: 100/day. Default 500 for safety.`

2. **`subscriber/digest.py`** (line ~40):
   - Current: `GMAIL_DAILY_LIMIT = 90  # Safety cap (Gmail hard limit is 100/day)`
   - Change to: `DAILY_EMAIL_LIMIT = int(os.environ.get("DAILY_EMAIL_LIMIT", "500"))  # Resend Pro: no limit. Safety cap.`
   - Update all references to `GMAIL_DAILY_LIMIT` in the file to use `DAILY_EMAIL_LIMIT`
   - Update the warning message at line ~228 to say "Daily email limit" instead of "Gmail daily limit"
   - Add `import os` if not already imported
  </action>
  <verify>
  Run `grep -n "GMAIL_DAILY_LIMIT\|gmail.*limit\|Gmail.*limit" subscriber/router.py subscriber/digest.py` -- should return 0 results (all references removed).
  Run `grep -n "DAILY_EMAIL_LIMIT" subscriber/router.py subscriber/digest.py` -- should show the new constant in both files.
  Run `python -c "from subscriber.digest import DAILY_EMAIL_LIMIT; print(DAILY_EMAIL_LIMIT)"` -- prints 500.
  </verify>
  <done>
  Gmail 90/day safety limit replaced with configurable DAILY_EMAIL_LIMIT (default 500) in both router.py and digest.py. No Gmail-specific references remain.
  </done>
</task>

<task type="auto">
  <name>Task 2: Refactor welcome email workflow + add Resend secrets to all workflows</name>
  <files>.github/workflows/send_welcome_email.yml, .github/workflows/find_deals.yml, .github/workflows/priority_monitor.yml, .github/workflows/premium_cabin_monitor.yml, .github/workflows/weekly_digest.yml, .github/workflows/mistake_fares.yml</files>
  <action>
1. **Refactor `.github/workflows/send_welcome_email.yml`:**
   - Replace the entire inline Python SMTP code block (lines 32-113) with:
     ```yaml
     - name: Install dependencies
       run: pip install -r requirements.txt

     - name: Send welcome email
       env:
         SMTP_EMAIL: ${{ secrets.SMTP_EMAIL }}
         SMTP_PASSWORD: ${{ secrets.SMTP_PASSWORD }}
         RESEND_API_KEY: ${{ secrets.RESEND_API_KEY }}
         UNSUBSCRIBE_SECRET: ${{ secrets.UNSUBSCRIBE_SECRET }}
         SEND_DOMAIN: ${{ vars.SEND_DOMAIN || 'dettyflightdeals.com' }}
         UNSUBSCRIBE_BASE_URL: ${{ vars.UNSUBSCRIBE_BASE_URL || 'https://unsubscribe.dettyflightdeals.com' }}
       run: |
         python -c "
         from mvp0_sender import send_welcome_email
         import os
         email = os.environ['RECIPIENT']
         name = os.environ.get('RECIPIENT_NAME', '')
         success = send_welcome_email(email, name)
         exit(0 if success else 1)
         "
     ```
   - Keep the `RECIPIENT` and `RECIPIENT_NAME` env vars from the workflow_dispatch inputs
   - Add `pip install -r requirements.txt` step before the send step

2. **Add Resend secrets to all 5 other email-sending workflows:**
   For each of these files, find the step(s) that run Python scripts with email functionality and add these env vars to the `env:` block:
   ```yaml
   RESEND_API_KEY: ${{ secrets.RESEND_API_KEY }}
   UNSUBSCRIBE_SECRET: ${{ secrets.UNSUBSCRIBE_SECRET }}
   SEND_DOMAIN: ${{ vars.SEND_DOMAIN || 'dettyflightdeals.com' }}
   UNSUBSCRIBE_BASE_URL: ${{ vars.UNSUBSCRIBE_BASE_URL || 'https://unsubscribe.dettyflightdeals.com' }}
   ```

   **Files to update:**
   - `.github/workflows/find_deals.yml` - add to the step that runs deal_finder.py
   - `.github/workflows/priority_monitor.yml` - add to the step that runs amadeus_monitor.py
   - `.github/workflows/premium_cabin_monitor.yml` - add to the step that runs premium_cabin_monitor.py
   - `.github/workflows/weekly_digest.yml` - add to the step that runs subscriber.digest
   - `.github/workflows/mistake_fares.yml` - add to the step that runs mistake_fare_monitor.py

   **IMPORTANT:** Keep existing SMTP_EMAIL and SMTP_PASSWORD secrets in place (they're the fallback). Do NOT remove them. Just ADD the Resend secrets alongside them.

   **Use `${{ vars.SEND_DOMAIN }}` (repository variable) with fallback**, not `${{ secrets.SEND_DOMAIN }}` -- domain name is not secret and repository variables are easier to manage.
  </action>
  <verify>
  Run `grep -l "RESEND_API_KEY" .github/workflows/*.yml | wc -l` -- should be 6 (all email-sending workflows).
  Run `grep -l "UNSUBSCRIBE_SECRET" .github/workflows/*.yml | wc -l` -- should be 6.
  Run `grep "mvp0_sender" .github/workflows/send_welcome_email.yml` -- confirms module-based approach (not inline SMTP).
  Run `grep "smtplib" .github/workflows/send_welcome_email.yml` -- should return 0 results (inline SMTP removed).
  Run `grep "SMTP_EMAIL" .github/workflows/find_deals.yml` -- still present (fallback preserved).
  </verify>
  <done>
  Welcome email workflow refactored to use mvp0_sender module. All 6 email-sending workflows have RESEND_API_KEY, UNSUBSCRIBE_SECRET, SEND_DOMAIN, and UNSUBSCRIBE_BASE_URL env vars. SMTP credentials preserved as fallback.
  </done>
</task>

</tasks>

<verification>
1. `grep -rn "GMAIL_DAILY_LIMIT" subscriber/` returns 0 results
2. `grep -c "RESEND_API_KEY" .github/workflows/find_deals.yml .github/workflows/priority_monitor.yml .github/workflows/premium_cabin_monitor.yml .github/workflows/weekly_digest.yml .github/workflows/mistake_fares.yml .github/workflows/send_welcome_email.yml` -- all show >= 1
3. `grep "smtplib" .github/workflows/send_welcome_email.yml` returns 0 (no inline SMTP)
4. `grep "SMTP_EMAIL" .github/workflows/find_deals.yml` returns >= 1 (fallback preserved)
5. `grep "DAILY_EMAIL_LIMIT" subscriber/router.py subscriber/digest.py` shows new constant
</verification>

<success_criteria>
- Gmail 90/day safety limit replaced with configurable 500/day default
- Welcome email workflow uses mvp0_sender.send_welcome_email() instead of inline SMTP code
- All 6 workflows have RESEND_API_KEY, UNSUBSCRIBE_SECRET, SEND_DOMAIN, UNSUBSCRIBE_BASE_URL
- SMTP_EMAIL/SMTP_PASSWORD preserved as fallback in all workflows
- No inline smtplib code remains in send_welcome_email.yml
</success_criteria>

<output>
After completion, create `.planning/phases/07-email-delivery-scale/07-04-SUMMARY.md`
</output>
