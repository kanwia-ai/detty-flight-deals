# fast-flights MUST stay pinned: v3.0 (2026-06-13) changed the API and broke
# imports, which silently killed the daily cron for 3 weeks. Its transitive
# deps must stay pinned too — newer primp drops the chrome_126 impersonation
# profile and Google then serves a JS shell with no prices. Bump deliberately.
fast-flights==2.2
typing-extensions==4.15.0
primp==1.3.1
protobuf==7.35.1
selectolax==0.4.10
feedparser>=6.0.0
requests>=2.28.0
gspread>=5.0.0
google-auth>=2.0.0
