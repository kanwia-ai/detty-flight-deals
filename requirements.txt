fast-flights>=1.0.0
feedparser>=6.0.0
requests>=2.28.0
gspread>=5.0.0
google-auth>=2.0.0
amadeus>=12.0.0

# Turso/libSQL database
# Note: libsql requires cmake for building from source (GitHub Actions has cmake pre-installed)
# For local development on Mac: brew install cmake
libsql>=0.1.11
tenacity>=8.0.0

# Anomaly detection (Phase 3)
scipy>=1.11.0
numpy>=1.24.0
pandas>=2.0.0
